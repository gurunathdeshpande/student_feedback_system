{
  "name": "student-feedback-devops",
  "version": "1.0.0",
  "description": "Student Feedback System",
  "main": "index.js",
  "scripts": {
    "start": "cross-env PORT=4000 concurrently \"cd backend && cross-env PORT=4000 npm run dev\" \"cd frontend && cross-env PORT=3000 npm start\"",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
} 