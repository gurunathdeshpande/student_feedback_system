services:
  # Backend API service
  - type: web
    name: student-feedback-backend
    env: node
    region: ohio
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: JWT_EXPIRES_IN
        value: 24h
      - key: FRONTEND_URL
        value: https://student-feedback-frontend-bbir.onrender.com
    autoDeploy: true
    domains:
      - student-feedback-api.onrender.com

  # Frontend static site (deployed separately)
  - type: static
    name: student-feedback-frontend
    env: static
    region: ohio
    buildCommand: cd frontend && npm install && CI=false npm run build
    publishDir: frontend/build
    envVars:
      - key: REACT_APP_API_URL
        value: https://student-feedback-api.onrender.com/api
      - key: NODE_ENV
        value: production
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    domains:
      - student-feedback-frontend.onrender.com 